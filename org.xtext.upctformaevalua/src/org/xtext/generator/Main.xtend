/*
 * generated by Xtext 2.17.1
 */
package org.xtext.generator

import com.google.inject.Inject
import com.google.inject.Provider
import org.eclipse.emf.common.util.URI
import org.eclipse.emf.ecore.resource.ResourceSet
import org.eclipse.xtext.generator.GeneratorContext
import org.eclipse.xtext.generator.GeneratorDelegate
import org.eclipse.xtext.generator.JavaIoFileSystemAccess
import org.eclipse.xtext.util.CancelIndicator
import org.eclipse.xtext.validation.CheckMode
import org.eclipse.xtext.validation.IResourceValidator
import org.xtext.UpctformaevaluaStandaloneSetup

class MainEvalua {

	private static MainEvalua instance;
	
	def static main(String[] args) {
		if (args.empty) {
			System::err.println('Aborting: no path to EMF resource provided!')
			return
		}
		val injector = new UpctformaevaluaStandaloneSetup().createInjectorAndDoEMFRegistration
		val mainevalua = injector.getInstance(MainEvalua)
		mainevalua.runGenerator(args.get(0),args.get(1))
	}

	def public static run(String string, String salida) {
		val injector = new UpctformaevaluaStandaloneSetup().createInjectorAndDoEMFRegistration
		val mainevalua = injector.getInstance(MainEvalua)
		mainevalua.runGenerator(string, salida);
		
	}

	def public static synchronized MainEvalua get() {
		if (instance === null) {
			val injector = new UpctformaevaluaStandaloneSetup().createInjectorAndDoEMFRegistration
			instance = injector.getInstance(MainEvalua);
		}
		return instance;
	}
	
	@Inject Provider<ResourceSet> resourceSetProvider

	@Inject IResourceValidator validator

	@Inject GeneratorDelegate generator

	@Inject JavaIoFileSystemAccess fileAccess

	def public runGenerator(String string,String salida) {
		// Load the resource
		val set = resourceSetProvider.get
		val resource = set.getResource(URI.createFileURI(string), true)

		// Validate the resource
		val issues = validator.validate(resource, CheckMode.ALL, CancelIndicator.NullImpl)
		if (!issues.empty) {
			issues.forEach[System.err.println(it)]
			return
		}

		// Configure and start the generator
		fileAccess.outputPath = salida
		val context = new GeneratorContext => [
			cancelIndicator = CancelIndicator.NullImpl
		]
		generator.generate(resource, fileAccess, context)
		System.out.println('Code generation finished.')
	}
}
